# Doom Perf engine patch: p_inter.c
#
# Cumulative Doom Perf changes to p_inter.c, consolidated from the original
# per-feature patch series. Bundles (original patch -> feature):
#   0003  disable-player-damage
#
# Order-independent: this is the only Doom Perf patch that touches p_inter.c.
# Regenerate by diffing a clean tree against the staged build tree;
# see scripts/build-doom-wasm.sh.

--- a/p_inter.c
+++ b/p_inter.c
@@ -790,6 +790,11 @@
     if (target->health <= 0)
 	return;
 
+    // Doom Perf: the observer is immune to all damage so exploration is
+    //  never interrupted by hazards (lava, crushers, etc.).
+    if (target->player)
+	return;
+
     if ( target->flags & MF_SKULLFLY )
     {
 	target->momx = target->momy = target->momz = 0;
