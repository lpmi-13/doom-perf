# Doom Perf engine patch: p_map.c
#
# Cumulative Doom Perf changes to p_map.c, consolidated from the original
# per-feature patch series. Bundles (original patch -> feature):
#   0019  remove-use-grunt
#
# Order-independent: this is the only Doom Perf patch that touches p_map.c.
# Regenerate by diffing a clean tree against the staged build tree;
# see scripts/build-doom-wasm.sh.

--- a/p_map.c
+++ b/p_map.c
@@ -1101,10 +1101,11 @@
 	P_LineOpening (in->d.line);
 	if (openrange <= 0)
 	{
-	    S_StartSound (usething, sfx_noway);
-	    
+	    // Doom Perf: suppress the "no way" grunt when USE meets a solid wall.
+	    // The browser opens a terminal overlay on USE near the diagnostic
+	    // screens, so the original wall-bump grunt is an unwanted holdover.
 	    // can't use through a wall
-	    return false;	
+	    return false;
 	}
 	// not a special line, but keep checking
 	return true ;		
