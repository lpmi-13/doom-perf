--- a/v_video.c
+++ b/v_video.c
@@ -253,7 +253,7 @@
 			 
 	    while (count--) 
 	    { 
-		*dest = *source++; 
+		{ byte _ts = *source++; *dest = doomperf_title_remap ? doomperf_title_lut[_ts] : _ts; }
 		dest += SCREENWIDTH; 
 	    } 
 	    column = (column_t *)(  (byte *)column + column->length 
@@ -318,7 +318,7 @@
 			 
 	    while (count--) 
 	    { 
-		*dest = *source++; 
+		{ byte _ts = *source++; *dest = doomperf_title_remap ? doomperf_title_lut[_ts] : _ts; }
 		dest += SCREENWIDTH; 
 	    } 
 	    column = (column_t *)(  (byte *)column + column->length 
@@ -384,7 +384,7 @@
  
 	    while (count--) 
 	    { 
-		*dest = *source++; 
+		{ byte _ts = *source++; *dest = doomperf_title_remap ? doomperf_title_lut[_ts] : _ts; }
 		dest += SCREENWIDTH/4; 
 	    } 
 	    column = (column_t *)(  (byte *)column + column->length 
--- a/d_main.c
+++ b/d_main.c
@@ -457,7 +457,10 @@
 //
 void D_PageDrawer (void)
 {
+    int titleremap = pagename && !strcmp(pagename, "TITLEPIC");
+    if (titleremap) { DoomPerf_UpdateTitleLut(); doomperf_title_remap = 1; }
     V_DrawPatch (0,0, 0, W_CacheLumpName(pagename, PU_CACHE));
+    doomperf_title_remap = 0;
 }
 
 
--- a/m_menu.c
+++ b/m_menu.c
@@ -891,7 +891,10 @@
 //
 void M_DrawMainMenu(void)
 {
+    DoomPerf_UpdateTitleLut();
+    doomperf_title_remap = 1;
     V_DrawPatchDirect (94,2,0,W_CacheLumpName("M_DOOM",PU_CACHE));
+    doomperf_title_remap = 0;
 }
 
 
